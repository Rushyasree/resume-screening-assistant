services:
  - type: web
    name: resume-backend
    env: python
    plan: free
    buildCommand: ""
    startCommand: python backend/main.py
    envVars:
      - key: PORT
        value: 5000
    autoDeploy: true
